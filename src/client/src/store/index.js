import Vue from 'vue'
import Vuex from 'vuex'

import tree from './tree/tree'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    tree
  }
})
