const tree = require('../dummyData/knowledgeNetwork.json')

class TreeManager {
    constructor() {
        this.tree = tree
    }

    getNode(id) {
        this.tree()
        for (let node of this.tree) {

        }
    }
}